# Updated_AI_Engine_v2 — Merged dependencies
# Install with: pip install -r requirements.txt

# Core vision & ML
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0

# Object detection
ultralytics>=8.0.0          # YOLOv8

# Multi-object tracking
deep-sort-realtime>=1.3.2   # DeepSORT

# Person re-identification (osnet_ain_x1_0)
torchreid>=1.4.0            # OSNet-AIN — install: pip install torchreid
                             # (or: pip install git+https://github.com/KaiyangZhou/deep-person-reid.git)

# Cross-camera matching
faiss-cpu>=1.7.4            # swap to faiss-gpu if FAISS_USE_GPU=True

# Theft detection (X3D-S model)
pytorchvideo>=0.1.5         # X3D-S via torch.hub facebookresearch/pytorchvideo
